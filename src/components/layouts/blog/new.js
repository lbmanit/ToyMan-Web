import React from 'react';
import LazyLoad from 'react-lazyload';
import Spinner from '../../../Spinner';

function New(blogs) {
  // const news = blogs.map(new => {
  //   return <article>
  //     <LazyLoad height={563} offset={100} once placeholder={<Spinner/>}>
  //       <img src={new.image} alt={new.title} />
  //     </LazyLoad>
  //     <h1>{new.title}</h1>
  //     <p>{new.content}</p>
  //     <p>{new.dateUpLoad}</p>
  //   </article>
  // })
}

export default New;
